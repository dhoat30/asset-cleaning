/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://www.assetcleaning.co.nz',
    generateRobotsTxt: true, // (optional)
    // ...other options
}